{
  "name": "dataplane",
  "version": "0.0.1",
  "description": "Darx data plan server",
  "private": true,
  "scripts": {
    "build": "cargo build",
    "test": "cargo test-all-features",
    "lint": "cargo clippy -- -Dwarnings --no-deps",
    "dev": "cargo run -- server && cargo run -- dev ./tmp"
  }
}
